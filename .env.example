# Medusa v2 - Railway environment example
# Copy these into Railway Variables (do not commit secrets)

# Core
NODE_ENV=production
# PORT is provided by Railway automatically; do not override
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
REDIS_URL=redis://default:PASSWORD@HOST:6379

# HTTP/CORS
ADMIN_CORS=*
STORE_CORS=*
AUTH_CORS=*
JWT_SECRET=replace_with_random_string
COOKIE_SECRET=replace_with_random_string

# Runtime mode
# server | worker | shared
MEDUSA_WORKER_MODE=shared
# Set to your Railway public URL once deployed
MEDUSA_BACKEND_URL=https://your-railway-app-url

# Optional caching module (reuse Redis)
CACHE_REDIS_URL=${REDIS_URL}

# Optional providers
# STRIPE_API_KEY=sk_live_...
# S3_ACCESS_KEY_ID=...
# S3_SECRET_ACCESS_KEY=...
# S3_REGION=us-east-1
# S3_BUCKET=your-bucket
# S3_ENDPOINT=https://s3.amazonaws.com
